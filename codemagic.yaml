workflows:
  ios-build:
    name: Build para iOS
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - flutter clean
      - flutter pub get
      
    artifacts:
      - build/ios/ipa/*.ipa
