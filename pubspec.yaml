name: metty_driver_novo
description: App para motoristas METY
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.2.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Dependências para API
  http: ^1.6.0
  shared_preferences: ^2.5.4
  
  # Dependências para mapa e ligações
  google_maps_flutter: ^2.5.0
  url_launcher: ^6.2.1
  
  # Ícones
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
